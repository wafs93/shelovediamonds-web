import {productType} from './productType'
import {settingsType} from './settingsType'
import {scheduledPostType} from './scheduledPostType'

export const schemaTypes = [productType, settingsType, scheduledPostType]
